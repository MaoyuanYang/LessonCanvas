# F012 Deployment Evidence Record (Spec D5)

> Operational record, not business truth. Every row binds a command that
> actually executed, its date, and its real outcome.

Environment: owner machine `192.168.9.101`, stack `infra/docker-compose.yml --profile app --env-file infra/deploy.env` (Web :3002 on LAN, API :8000, Celery worker, PostgreSQL/pgvector, Redis, MinIO; DeepSeek live key; ADR-0006 guest workspace tokens, no login). Infra passwords: random-generated into untracked `infra/deploy.env` (mode 600). Docker access: `ymy` added to `docker` group by owner (sudo) 2026-09-02; commands run via `sg docker -c ...`.

| # | Step | Command (executed form) | Date | Outcome | Evidence |
| --- | --- | --- | --- | --- | --- |
| 1 | Image build | `sg docker -c "docker compose -f infra/docker-compose.yml --profile app --env-file infra/deploy.env build"` (via `deploy.sh` step 1) | 2026-09-02 | PASS — `lessoncanvas/api:local`, `lessoncanvas/web:local` built (after `.dockerignore` + standalone-layout fixes, see review IF-8/IF-9 notes) | build log; images present locally |
| 2 | Migration (api entrypoint `alembic upgrade head` + `python -m lessoncanvas.checkpointer_setup`) | `deploy.sh` step 2 (api container CMD) | 2026-09-02 | PASS — migrations incl. `f012a7c9d2e4` (subject rename) applied; checkpointer pre-setup "ok (attempt 1)" in api and worker logs | `docker logs infra-api-1`, `docker logs infra-worker-1` |
| 3 | Stack start + health | `sg docker -c "infra/scripts/deploy.sh"` | 2026-09-02 | PASS — all six services `(healthy)`; wait-loop exited "All services healthy." | deploy.sh output; `docker compose ps` |
| 4 | Smoke | `infra/scripts/smoke.sh` (deploy step 4) | 2026-09-02 | PASS — `api /health {"status":"ok","database":"ok"}`; `web / HTTP 200`; LAN `http://192.168.9.101:3002` HTTP 200 | deploy.sh output |
| 5 | Sample seeding (idempotent) | `docker compose ... exec -e LESSONCANVAS_MODEL_ADAPTER=fake -e LESSONCANVAS_TASKS_EAGER=true api python scripts/seed_sample.py` — run 3× total (initial stack + idempotency re-run + post-redeploy) | 2026-09-02 | PASS — seeded (`project_id 01a0610e-ff0b-7228-8fb5-47eb4148a44f`, brief/blueprint versions + 5 run ids recorded); re-run returned `{"seeded": false, "already_present": true}`; post-teardown redeploy re-seeded from empty; exits 0 | seed JSON outputs above |
| 6 | Representative journey incl. recovery (TS-007/TS-029, live DeepSeek) | `E2E_BASE_URL=http://localhost:3002 E2E_GEN_LIVE=1 npx playwright test e2e/generation-journeys.spec.ts --grep TS-029` | 2026-09-02 | PASS (51.7s) — project → 访谈 → 简报确认 → 蓝图 → 生成中离开/返回/刷新恢复权威进度 → 全部课时生成 → 下载;单次运行、限额内 | Playwright run; api/worker logs |
| 7 | Deletion completeness spot-check (TS-008) | scripted API journey: guest token → create project `01a0610b-a399-7706-9774-51fd7795552d` + source upload → `DELETE /account` → `{"purged": true}`; then psql + boto3 verification | 2026-09-02 | PASS — workspaces/projects/sources/checkpoints rows for subject: 0/0/0/0; MinIO objects under subject prefix: 0 in both buckets; content-free `retained_security_events` survives (5 rows) | psql/boto3 outputs above |
| 8 | SSE single-process + cap (TS-010) | `docker top infra-api-1` | 2026-09-02 | PASS — exactly one uvicorn python process (SSE registry assumption holds; replicas: 1 documented in compose) | docker top output |
| 9 | F011 D10 provider constraints (TS-012, ADR-0006-revised) | checklist vs deployed config | 2026-09-02 | PASS — per-store deletion reachability proven by row 7 on the deployed stores; private-object access: tokenless requests 401 on `/projects/{sample}` and business routes; token verification exercised by every journey; disclosure wording rendered and asserted by `guardrails.spec.ts` on the deployed stack; identity is application-issued (no external IdP constraint remains) | rows 4/6/7 + deployed guardrails E2E |
| 10 | Reviewer bounded-generation journey (TS-013) | row 6 journey ran as an anonymous reviewer workspace under F011 limits; `e2e/guardrails.spec.ts` (deployed) asserts usage limits/audit surfaces | 2026-09-02 | PASS — within-limit journey completed; limits numerically unchanged (deterministic suite); usage/audit visible | rows 4/6 |
| 11 | Teardown + redeploy from clean state (TS-002) | `echo y | sg docker -c "infra/scripts/teardown.sh"` → `sg docker -c "infra/scripts/deploy.sh"` → re-seed (row 5) | 2026-09-02 | PASS — volumes/containers removed; redeploy reached healthy + smoke pass; seed reproduced from empty state | teardown/deploy outputs |
| 12 | Accessibility + reduced-screen spot check (TS-015/T9) | scripted Playwright probe on deployed stack: keyboard-only landing→CTA→/projects; `/sample` tabs + read-only notice; 420px viewport | 2026-09-02 | PASS after IF-9 fix — keyboard focus reaches 进入备课工作台, Enter enters /projects; sample notice + all tabs render (evidence runs listed with zh labels, honest 尚未运行技术评估 + product-validation disclosure); 420px: no page-level horizontal overflow (`scrollW 420 == clientW 420`); /projects,/account clean | probe outputs above |
| 13 | Web/API-level guardrails E2E on deployed stack | `E2E_BASE_URL=http://localhost:3002 npx playwright test e2e/public.spec.ts e2e/guardrails.spec.ts` | 2026-09-02 | PASS 5/5 | Playwright run |
| 14 | Deployed sample read + write-denial API checks | guest token → `GET /sample` + `GET /projects/{id}` 200; `POST generation/start` 404; tokenless 401 | 2026-09-02 | PASS | curl outputs above |

Deployed-verification defect found and fixed during T8 (recorded in `review.md`):

- `IF-8` (High): `PostgresSaver.setup()` first-use DDL deadlock in the multi-process deployed topology blocked discovery start (observed live; `pg_stat_activity` showed `CREATE INDEX CONCURRENTLY` queueing behind open transactions). Fix: `lessoncanvas.checkpointer_setup` runs serially in the api/worker entrypoints with `lock_timeout` + bounded retries; after the fix the live journey passes.
- `IF-9` (Low): workspace tabs nav overflowed at 420px (pre-existing shared component surfaced by `/sample`); fixed with `flex-wrap`; deployed re-verified.
